{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "dependsOn": [
                "^build"
            ],
            "outputs": [
                ".next/**",
                "dist/**"
            ],
            "env": [
                "DATABASE_URL",
                "JWT_SECRET",
                "AUTH_SECRET",
                "AUTH_URL",
                "AUTH_TRUST_HOST",
                "AUTH_GOOGLE_ID",
                "AUTH_GOOGLE_SECRET",
                "AUTH_GITHUB_ID",
                "AUTH_GITHUB_SECRET"
            ]
        },
        "dev": {
            "cache": false
        }
    }
}
